import styled from "styled-components";
import {Pagination as BootstrapPagination} from "react-bootstrap";

export const StyledPagination = styled(BootstrapPagination)`
  display: flex;
  justify-content: center;
  margin-top: 10px;
`